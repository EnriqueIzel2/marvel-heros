import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  Card: {
    flexDirection: 'row',
    padding: 10,
    alignItems: 'center',
  },
  Avatar: {
    height: 50,
    width: 50,
    borderRadius: 25,
  },
  Name: {marginLeft: 10},
});

export default styles;
